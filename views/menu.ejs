<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menú Principal - Admin</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700;900&family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            font-family: 'Inter', sans-serif;
            background: #0a0e27;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            position: relative;
            overflow-x: hidden;
        }
        
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                radial-gradient(circle at 20% 30%, rgba(255, 107, 107, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 80% 70%, rgba(78, 205, 196, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 50% 50%, rgba(255, 195, 113, 0.1) 0%, transparent 50%);
            pointer-events: none;
            animation: shift 20s ease-in-out infinite;
        }
        
        @keyframes shift {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }
        
        /* Header */
        .header {
            padding: 2rem 3rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: relative;
            z-index: 10;
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }
        
        .logo {
            color: #fff;
            font-size: 1.5rem;
            font-weight: 300;
            letter-spacing: 0.2em;
            text-transform: uppercase;
            font-family: 'Inter', sans-serif;
        }
        
        .btn-logout {
            background: transparent;
            color: #4ecdc4;
            border: 1px solid #4ecdc4;
            padding: 0.7rem 1.8rem;
            border-radius: 0;
            cursor: pointer;
            font-size: 0.85rem;
            font-weight: 600;
            letter-spacing: 0.1em;
            text-transform: uppercase;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            text-decoration: none;
            display: inline-block;
            position: relative;
            overflow: hidden;
        }
        
        .btn-logout::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: #4ecdc4;
            transition: left 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            z-index: -1;
        }
        
        .btn-logout:hover::before {
            left: 0;
        }
        
        .btn-logout:hover {
            color: #0a0e27;
            box-shadow: 0 0 20px rgba(78, 205, 196, 0.4);
        }
        
        /* Contenedor principal */
        .main-container {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 3rem;
            position: relative;
            z-index: 1;
        }
        
        .menu-container {
            text-align: center;
            max-width: 1200px;
        }
        
        h1 {
            color: #fff;
            font-size: 4.5rem;
            margin-bottom: 1rem;
            font-family: 'Playfair Display', serif;
            font-weight: 900;
            line-height: 1.1;
            letter-spacing: -0.02em;
            animation: fadeInUp 0.8s cubic-bezier(0.4, 0, 0.2, 1);
        }
        
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .subtitle {
            color: rgba(255, 255, 255, 0.5);
            font-size: 1.1rem;
            margin-bottom: 4rem;
            font-weight: 300;
            letter-spacing: 0.05em;
            animation: fadeInUp 0.8s cubic-bezier(0.4, 0, 0.2, 1) 0.2s backwards;
        }
        
        .menu-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 2rem;
            max-width: 1100px;
            margin: 0 auto;
        }
        
        .menu-card {
            background: rgba(255, 255, 255, 0.02);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 0;
            padding: 3rem 2.5rem;
            text-decoration: none;
            color: #fff;
            transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
            cursor: pointer;
            position: relative;
            overflow: hidden;
            backdrop-filter: blur(10px);
            animation: fadeInUp 0.8s cubic-bezier(0.4, 0, 0.2, 1) backwards;
        }
        
        .menu-card:nth-child(1) { animation-delay: 0.3s; }
        .menu-card:nth-child(2) { animation-delay: 0.4s; }
        .menu-card:nth-child(3) { animation-delay: 0.5s; }
        
        .menu-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.05), transparent);
            transition: left 0.6s;
        }
        
        .menu-card:hover::before {
            left: 100%;
        }
        
        .menu-card:hover {
            transform: translateY(-8px);
            border-color: rgba(255, 255, 255, 0.2);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4);
        }
        
        .menu-icon {
            font-size: 4rem;
            margin-bottom: 1.5rem;
            display: block;
            opacity: 0.9;
            transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
        }
        
        .menu-card:hover .menu-icon {
            transform: scale(1.1) rotate(5deg);
        }
        
        .menu-title {
            font-size: 1.8rem;
            font-weight: 600;
            margin-bottom: 1rem;
            color: #fff;
            font-family: 'Playfair Display', serif;
            letter-spacing: -0.01em;
        }
        
        .menu-description {
            font-size: 0.95rem;
            color: rgba(255, 255, 255, 0.5);
            line-height: 1.6;
            font-weight: 300;
        }
        
        .card-eventos { border-top: 3px solid #ff6b6b; }
        .card-foros { border-top: 3px solid #4ecdc4; }
        .card-videos { border-top: 3px solid #ffc371; }
        
        .card-eventos:hover { border-top-color: #ff5252; box-shadow: 0 20px 60px rgba(255, 107, 107, 0.2); }
        .card-foros:hover { border-top-color: #45b7af; box-shadow: 0 20px 60px rgba(78, 205, 196, 0.2); }
        .card-videos:hover { border-top-color: #ffb84d; box-shadow: 0 20px 60px rgba(255, 195, 113, 0.2); }
        
        /* Responsive */
        @media (max-width: 768px) {
            h1 { font-size: 2.5rem; }
            .subtitle { font-size: 1rem; }
            .menu-grid { grid-template-columns: 1fr; }
            .header { padding: 1.5rem 1.5rem; flex-direction: column; gap: 1rem; }
            .logo { font-size: 1.2rem; }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <div class="header">
        <div class="logo">Admin Panel</div>
        <a href="/logout" class="btn-logout">Cerrar Sesión</a>
    </div>

    <!-- Contenido Principal -->
    <div class="main-container">
        <div class="menu-container">
            <h1>Sistema de Gestión</h1>
            <p class="subtitle">Selecciona el módulo que deseas gestionar</p>
            
            <div class="menu-grid">
                <!-- Gestión de Eventos -->
                <a href="/eventos" class="menu-card card-eventos">
                    <span class="menu-icon">📅</span>
                    <div class="menu-title">Gestión de Eventos</div>
                    <div class="menu-description">
                        Administra y actualiza la información de eventos, departamentos y municipios
                    </div>
                </a>
                
                <!-- Gestión de Foros -->
                <a href="/foros" class="menu-card card-foros">
                    <span class="menu-icon">💬</span>
                    <div class="menu-title">Gestión de Foros</div>
                    <div class="menu-description">
                        Modera y gestiona los foros de discusión de la plataforma
                    </div>
                </a>
                
                <!-- Gestión de Videos -->
                <a href="/videos" class="menu-card card-videos">
                    <span class="menu-icon">🎬</span>
                    <div class="menu-title">Gestión de Videos</div>
                    <div class="menu-description">
                        Administra el contenido multimedia y videos de la plataforma
                    </div>
                </a>
            </div>
        </div>
    </div>
</body>
</html>